/**
 * @description 全局变量
 */
var Label = {};
var curWwwPath=window.document.location.href;
var pathName=window.document.location.pathname;
var pos=curWwwPath.indexOf(pathName);
var localhostPaht=curWwwPath.substring(0,pos);
if(pathName=="/"){
    Label.servePath = curWwwPath.substring(0,curWwwPath.length-1)
}else{
    Label.servePath = localhostPaht;
}

var Util = {
    goLogin: function () {
        if (-1 === location.href.indexOf("/login")) {
           // var e = location.href;
           // 0 === location.search.indexOf("?goto") && (e = location.href.replace(location.search, "")),
                window.location.href = Label.servePath + "/login";
        }
    },
    isLogin: function () {
        
    },
    getUsername:function () {

    }
}